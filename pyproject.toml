[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "lumache"
authors = [{name = "Célia Desgrange", email = "celia.desgrange@eso.org"}]
dynamic = ["version", "description"]
